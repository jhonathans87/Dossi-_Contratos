services:
  - type: web
    name: dossie-imobiliario
    runtime: docker
    plan: starter
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: OPENAI_MODEL
        value: gpt-5-mini
